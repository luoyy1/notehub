# NoteHub 迭代记录

## 当前版本

- 版本：v0.2.0 planning
- 日期：2026-05-18
- 仓库：https://github.com/luoyy1/notehub
- 基线提交：`1020553 Initial NoteHub baseline`

## 当前状态

- 前端：Vue 3 + Vite，包含首页、时间轴、功能规划、愿望清单等页面。
- 后端：Spring Boot，提供事件、日历标记、愿望清单、动态路由接口。
- 本地运行：
  - 前端默认端口：`3000`
  - 后端默认端口：`18080`
  - 启动脚本：`dev.cmd`
  - 检查脚本：`check.cmd`
- 已确认：
  - 前端类型检查可通过。
  - 前端构建可通过，但 Vite 8/Rolldown 在 Windows 路径下偶发构建路径问题。
  - 后端 Maven 生命周期可通过，目前还没有测试源码。
  - API 代理链路已跑通：`/api/routes`、`/api/events`、`/api/wishes`。

## 已知问题

- 时间轴页面存在布局遮挡问题，当前 CSS 调整未彻底解决，需要重新设计布局结构。
- Codex 内置浏览器主动 `goto()` 本地地址时可能报 `net::ERR_BLOCKED_BY_CLIENT`；可复用已打开的 app 标签页进行 DOM、点击和截图测试。
- 后端缺少自动化测试，当前 `mvn test` 显示 `No tests to run`。
- 前端接口 fallback 会静默使用 mock 数据，开发时不容易发现真实后端异常。

## 可视化测试方式

- 先确认 `dev.cmd` 已启动，`3000` 和 `18080` 都处于监听状态。
- 在 Codex 内置浏览器中优先复用已有的 `http://127.0.0.1:3000/` 标签页，不要重新 `goto()` 本地地址。
- 如需桌面视口，设置 viewport 为 `1280x800` 后再刷新当前标签。
- 可通过 DOM snapshot、导航按钮点击和 screenshot 完成页面冒烟。
- 已验证导航：首页、时间轴、功能规划、愿望清单均可点击并渲染主内容。

## 简略迭代计划

1. 事件中心快速编辑
   - 首页事件卡片增加编辑入口。
   - 点击后打开单事件编辑弹窗，不再进入批量配置后手动翻找。
   - 保存时沿用现有 `POST /api/events` 整包保存接口，保持后端 API 不变。
   - 保存成功后刷新首页卡片和日历数据。

2. 补测试基线
   - 后端先补 `DateUtil`、`CalculatorService` 单元测试。
   - 再补事件、愿望接口的 Controller 层测试。
   - 前端至少保留 `typecheck + build` 作为基础检查。

3. 优化本地开发体验
   - 固化 `dev.cmd`、`check.cmd`。
   - 处理 Vite 8/Rolldown 偶发构建问题，必要时锁定更稳定版本。
   - 开发环境区分 mock fallback 和真实接口错误。

4. 后续功能方向
   - 完善事件编辑体验。
   - 增强愿望清单的状态流转和筛选。
   - 增加数据备份/导入导出能力。

## v0.2.0 事件中心快速编辑

- 目标：从首页事件卡片直接编辑单个事件，降低日常维护成本。
- 范围：
  - 新增单事件编辑弹窗。
  - 支持名称、日期、分类、颜色、地点、心情、标签、故事、置顶、重复、正计时、倒计时、邮件提醒和提醒提前天数。
  - 保留现有“事件配置管家”批量管理能力。
- 接口策略：
  - 不新增后端 API。
  - 打开编辑时读取 `/api/events/raw`。
  - 保存时合并当前事件并调用 `POST /api/events` 保存完整列表。
- 验收标准：
  - 首页事件卡片能打开单事件编辑弹窗。
  - 修改故事、心情、置顶、标签后保存，首页和日历刷新。
  - 取消编辑不保存。
  - 控制台无 error。
